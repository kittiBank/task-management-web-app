{
  "name": "task-management-web-app",
  "private": true,
  "scripts": {
    "up": "docker compose up --build",
    "down": "docker compose down",
    "db:setup": "docker compose up --force-recreate db-init"
  }
}
